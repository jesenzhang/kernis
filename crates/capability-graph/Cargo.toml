[package]
name = "capability-graph"
description = "Capability, scope, and reactive plugin lifecycle authority for the Kernis runtime kernel."
repository = "https://github.com/jesenzhang/kernis"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
kernis-core = { path = "../core" }
tokio = { version = "1.45", features = ["sync", "rt", "macros"] }

[lints]
workspace = true
